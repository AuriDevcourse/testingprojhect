# Google OAuth Client ID for "Sign in with Google".
# Get one from Google Cloud Console — see docs/google-setup.md.
# Copy this file to `.env` and paste your client ID below. Restart `npm run dev` after.
VITE_GOOGLE_CLIENT_ID=
